public class MensagemFactory {
    private Mensagem mensagem;

    public Mensagem criarMensagem(String tipo, String conteudo){
        if (tipo.equals("texto")){
            mensagem = new 
        }
    }    
}